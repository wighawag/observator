{
	"name": "observator-monorepo",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"dependencies": {},
	"devDependencies": {},
	"scripts": {
		"format": "pnpm run -r format",
		"format:check": "pnpm run -r format:check",
		"test": "pnpm run -r test"
	}
}
